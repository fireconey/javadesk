def fun(arg ,gu):
一般的调用参数是按照顺序进行传递的，但是调用时使用关键字就可以不用管顺序了

如果函数设定了参数的默认值那么调用的时候可以选择性的传入参数 *********
def yu（name=“th",age=1):

有时不知道要传入的参数有多少，可以使用可变参数长度的函数**************
def yu（args1,*fage）后面的*fage是一个元组，如果调用的时候只有一个（元组为空），那么只使用args1，如果传入的有很多个那么所有的到会放在fage元组中

匿名函数（anonymouse）中不能有命令，有混合的表达式。调用的时候不能直接调用，只能使用表达式来调用（如赋值）。他只有一行，不能使用return
sum=lambda：1；
sum（）
